# SIGMA-Q v2

Protótipo evolutivo para a versão 2.0 do SIGMA-Q.
Estrutura, padrão de desenvolvimento e instruções básicas.

Como iniciar:
1. Criar virtualenv e ativar.
2. pip install -r requirements.txt
3. Executar `python -m pytest` para rodar testes.
